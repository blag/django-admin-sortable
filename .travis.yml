language: python

python:
    - "3.5"
    - "3.6"
    - "3.7"
    - "3.8"

env:
    - DJANGO_VERSION=2.2
    - DJANGO_VERSION=3.0

branches:
    only:
        - develop

matrix:
    exclude:
        -
            python: "3.5"
            env: DJANGO_VERSION=3.0

install:
    - pip install coveralls django==$DJANGO_VERSION

script:
    - coverage run manage.py test

after_success:
    - coveralls
